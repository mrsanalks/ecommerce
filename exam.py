# 1.write a python function that takes list of elements as argument and genarate a new list which contains factorial of each element?
# 2.print left angle trangle?
# 3.{vale:1,value:2,value:3,vale:4,value:5,value:6}
# from above dict fetch key value payers that only have even values
# 4['a','a','b','b','c'].
# pass the above list into a function a return a newlist by removing duplicate  values from list
# 5.[1,2,3,4,5] from the list find the min and max value using function with return?