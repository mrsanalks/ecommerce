# An exception is an error which occures during the excecution of a program and exception handling is a method that are used
# to catch and handle exceptions in python

# exceptions are classified into 2 types:

# 1)logic error;

#   these are errors that are created by the programmer
# example=syntax error,intendation error,name error

# 2)runtime error
#   these are errors that are created by the end users during  the program excecution
# example=zero division error,intex error,value error,file not found error





name=["ayshu","san","farzu"]


position=int(input("enter the position"))
print(name[position])